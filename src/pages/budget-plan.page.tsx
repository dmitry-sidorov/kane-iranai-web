import { BudgetPlan } from '@components';
import { Container } from '@mantine/core';

export const BudgetPlanPage = () => {
  return (
    <Container maw={640} py="xl">
      <BudgetPlan />
    </Container>
  );
};
